# Release 2026.09.12

| Field | Value |
|-------|-------|
| Version | `2026.09.12` |
| Date (local) | 2026-09-12 |
| Branch | `main` |
| Base ref | `origin/main` |
| Head commit | `d2340eb` |
| Author | Bearson-norm |
| Repo | PZEM-Project |
| Format | markdown |

## Summary

Broker/dashboard now matches PLN **invoice kWh** to 3-phase PZEM meter registers (T0→T1), without rupiah tariffs. CI runs the new pytest suite; CD packages `shared/` and schema SQL so the VPS actually receives ingest + migration 004.

## Included commits

- d2340eb feat: match PLN invoice kWh from 3-phase meter registers

## Features

- Ingest firmware `/data`, `/status`, `/heartbeat` with `meter_energy_kwh`, wrap/reset events, and idempotent replay
- Operator page `/pln-match` to log PLN readings and reconcile R+S+T against invoice kWh
- Quality flags: gap, wrap, reset, missing phase, unsynced time, error > 5%

## Fixes

- `.gitignore` no longer drops `*.sql` schema migrations (001–004 now tracked)
- Deploy package now includes `shared/` and verifies PLN bill-match files before upload

## Breaking changes

- None

## Database / migrations

- Applied on dashboard and mqtt-listener startup: `004_pln_bill_match.sql`
- Widens `pzem_data` (register + period + payload hash)
- Adds `pzem_energy_events`, `pzem_device_heartbeat`, `pln_readings`, `pln_reconciliations`
- Also tracked for the first time: `001_mqtt_canvas.sql`, `002_mqtt_bridge_name_unique.sql`, `003_pln_tariff_settings.sql`

## Config / environment

- Optional: `PLN_GAP_MINUTES` (default `20`) for sibling-live gap alerts
- No new GitHub secrets
- Existing `VPS_SSH_KEY` / `VPS_HOST` unchanged

## Deploy notes

- Push to `main` runs root `CI - Build and Test`, then `CD - Deploy to Production VPS` via `workflow_run`
- Package path: `pzem-monitoring/V9-Docker` → `/opt/pzem-monitoring`
- After containers start, dashboard applies 004; CD checks `/pln-match` and `pln_readings`
- Rupiah PDF reports and `/settings/pln-tariff` are unchanged

## Rollback

- Previous main: `62b2df5` (`adding documentation regarding how reports works`)
- Restore previous `/opt/pzem-monitoring` code backup under `~/.pzem-backups/`
- New tables can remain; ingest falls back to legacy `energy` columns if rolled back

## Verification checklist

- [ ] App starts / health check OK
- [ ] `GET /pln-match` returns the operator page
- [ ] `python -m pytest tests` passes (23 cases)
- [ ] `pln_readings` exists after dashboard startup
- [ ] Auth / role checks still pass
- [ ] Staging verified (if applicable)
- [ ] Production deploy confirmed via Actions CD

## Related

- PRs: —
- Issues: —
- Tags: v2026.09.12

## Traceability

| Item | Value |
|------|-------|
| Generated at | 2026-09-12T03:48:06.375Z |
| Generator | main-release-version-log |
| Commit range | `origin/main..d2340eb` |
